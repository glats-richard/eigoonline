---
export const prerender = false;

import Layout from "../../layouts/Layout.astro";
import { getCollection } from "astro:content";

const schools = await getCollection("schools");
const schoolsList = schools
  .map((s) => ({ id: s.id, name: s.data.name }))
  .sort((a, b) => a.name.localeCompare(b.name, "ja"));

const submitted = Astro.url.searchParams.get("submitted") === "true";
---

<Layout title="体験談を投稿" description="オンライン英会話サービスの体験談を投稿できます">
  <main class="mx-auto w-full max-w-3xl px-4 pb-14 pt-8">
    <div class="mb-8">
      <h1 class="text-2xl font-extrabold text-zinc-900">体験談を投稿</h1>
      <p class="mt-2 text-sm text-zinc-600">
        オンライン英会話サービスの体験談を投稿してください。投稿内容は管理者が確認後、承認されるとサイトに反映されます。
      </p>
    </div>

    {submitted ? (
      <div class="rounded-2xl border border-green-200 bg-green-50 p-6 text-center">
        <div class="text-lg font-extrabold text-green-900">投稿ありがとうございます</div>
        <p class="mt-2 text-sm text-green-700">
          体験談を送信しました。管理者が確認後、承認されるとサイトに反映されます。
        </p>
        <a
          href="/review/submit"
          class="mt-4 inline-block rounded-xl bg-green-600 px-6 py-2 text-sm font-extrabold text-white hover:bg-green-700"
        >
          もう1件投稿する
        </a>
      </div>
    ) : (
      <form method="post" action="/api/review/submit" class="space-y-6">
        <div class="rounded-2xl border border-zinc-200 bg-white p-6 shadow-sm">
          <label class="block">
            <div class="text-sm font-extrabold text-zinc-700">サービス <span class="text-rose-600">*</span></div>
            <select
              name="school_id"
              required
              class="mt-2 w-full rounded-lg border border-zinc-200 bg-white px-3 py-2 text-sm font-semibold text-zinc-900 shadow-sm focus:outline-none focus:ring-2 focus:ring-[color:var(--brand-link)]"
            >
              <option value="">選択してください</option>
              {schoolsList.map((s) => (
                <option value={s.id}>{s.name}</option>
              ))}
            </select>
          </label>
        </div>

        <div class="rounded-2xl border border-zinc-200 bg-white p-6 shadow-sm">
          <div class="text-sm font-extrabold text-zinc-700 mb-4">評価 <span class="text-rose-600">*</span></div>
          <div class="space-y-4">
            <label class="block">
              <div class="text-xs font-semibold text-zinc-600 mb-1">総合評価</div>
              <select
                name="overall_rating"
                required
                class="w-full rounded-lg border border-zinc-200 bg-white px-3 py-2 text-sm font-semibold text-zinc-900 shadow-sm focus:outline-none focus:ring-2 focus:ring-[color:var(--brand-link)]"
              >
                <option value="">選択してください</option>
                <option value="1">1.0</option>
                <option value="1.5">1.5</option>
                <option value="2">2.0</option>
                <option value="2.5">2.5</option>
                <option value="3">3.0</option>
                <option value="3.5">3.5</option>
                <option value="4">4.0</option>
                <option value="4.5">4.5</option>
                <option value="5">5.0</option>
              </select>
            </label>

            <label class="block">
              <div class="text-xs font-semibold text-zinc-600 mb-1">講師の質</div>
              <select
                name="teacher_quality"
                required
                class="w-full rounded-lg border border-zinc-200 bg-white px-3 py-2 text-sm font-semibold text-zinc-900 shadow-sm focus:outline-none focus:ring-2 focus:ring-[color:var(--brand-link)]"
              >
                <option value="">選択してください</option>
                <option value="1">1.0</option>
                <option value="1.5">1.5</option>
                <option value="2">2.0</option>
                <option value="2.5">2.5</option>
                <option value="3">3.0</option>
                <option value="3.5">3.5</option>
                <option value="4">4.0</option>
                <option value="4.5">4.5</option>
                <option value="5">5.0</option>
              </select>
            </label>

            <label class="block">
              <div class="text-xs font-semibold text-zinc-600 mb-1">教材の質</div>
              <select
                name="material_quality"
                required
                class="w-full rounded-lg border border-zinc-200 bg-white px-3 py-2 text-sm font-semibold text-zinc-900 shadow-sm focus:outline-none focus:ring-2 focus:ring-[color:var(--brand-link)]"
              >
                <option value="">選択してください</option>
                <option value="1">1.0</option>
                <option value="1.5">1.5</option>
                <option value="2">2.0</option>
                <option value="2.5">2.5</option>
                <option value="3">3.0</option>
                <option value="3.5">3.5</option>
                <option value="4">4.0</option>
                <option value="4.5">4.5</option>
                <option value="5">5.0</option>
              </select>
            </label>

            <label class="block">
              <div class="text-xs font-semibold text-zinc-600 mb-1">通信の質</div>
              <select
                name="connection_quality"
                required
                class="w-full rounded-lg border border-zinc-200 bg-white px-3 py-2 text-sm font-semibold text-zinc-900 shadow-sm focus:outline-none focus:ring-2 focus:ring-[color:var(--brand-link)]"
              >
                <option value="">選択してください</option>
                <option value="1">1.0</option>
                <option value="1.5">1.5</option>
                <option value="2">2.0</option>
                <option value="2.5">2.5</option>
                <option value="3">3.0</option>
                <option value="3.5">3.5</option>
                <option value="4">4.0</option>
                <option value="4.5">4.5</option>
                <option value="5">5.0</option>
              </select>
            </label>
          </div>
        </div>

        <div class="rounded-2xl border border-zinc-200 bg-white p-6 shadow-sm">
          <label class="block">
            <div class="text-sm font-extrabold text-zinc-700 mb-2">
              体験談・口コミ <span class="text-rose-600">*</span>
            </div>
            <textarea
              name="body"
              required
              minlength="10"
              maxlength="2000"
              rows="6"
              placeholder="サービスの体験談や口コミを記入してください（10文字以上2000文字以内）"
              class="w-full rounded-lg border border-zinc-200 bg-white px-3 py-2 text-sm font-semibold text-zinc-900 shadow-sm focus:outline-none focus:ring-2 focus:ring-[color:var(--brand-link)]"
            ></textarea>
            <div class="mt-1 text-xs text-zinc-500">10文字以上2000文字以内で入力してください</div>
          </label>
        </div>

        <div class="rounded-2xl border border-zinc-200 bg-white p-6 shadow-sm">
          <label class="block">
            <div class="text-sm font-extrabold text-zinc-700 mb-2">年齢（任意）</div>
            <input
              type="text"
              name="age"
              maxlength="20"
              placeholder="例: 30歳"
              class="w-full rounded-lg border border-zinc-200 bg-white px-3 py-2 text-sm font-semibold text-zinc-900 shadow-sm focus:outline-none focus:ring-2 focus:ring-[color:var(--brand-link)]"
            />
            <div class="mt-1 text-xs text-zinc-500">任意項目です</div>
          </label>
        </div>

        <div class="flex justify-end gap-3">
          <button
            type="submit"
            class="btn-brand inline-flex items-center justify-center rounded-xl px-6 py-3 text-sm font-extrabold text-white shadow-sm focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-orange-300 focus-visible:ring-offset-2 focus-visible:ring-offset-white"
          >
            投稿する
          </button>
        </div>
      </form>
    )}
  </main>
</Layout>
